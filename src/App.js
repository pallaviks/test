import React from 'react';
import CalculatorAgent from './CalculatorAgent';

const App = () => {
    return (
        <div>
            <h1>Calculator App</h1>
            <CalculatorAgent />
        </div>
    );
};

export default App;